setWidth = function (elem) {
  elem.classList.toggle("makeContainerBigger");
  elem.classList.toggle("makeContainerSmaller");

  elem.classList.toggle("smallerContainer");
  elem.classList.toggle("biggerContainer");
}
